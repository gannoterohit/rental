{
	"info": {
		"_postman_id": "roomrental-total-api",
		"name": "RoomRental TOTAL API Collection",
		"description": "EXHAUSTIVE Collection of ALL 78+ Endpoints covering Public, User, Owner, and Admin workflows.",
		"schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
	},
	"item": [
		{
			"name": "1. PUBLIC (No Auth)",
			"item": [
				{ "name": "Get App Settings", "request": { "method": "GET", "url": "{{base_url}}/settings" } },
				{ "name": "Auth: Send OTP", "request": { "method": "POST", "url": "{{base_url}}/auth/send-otp", "body": { "mode": "raw", "raw": "{\"email\":\"test@test.com\"}" } } },
				{ "name": "Auth: Register", "request": { "method": "POST", "url": "{{base_url}}/auth/register" } },
				{ "name": "Auth: Login", "request": { "method": "POST", "url": "{{base_url}}/auth/login" } },
				{ "name": "Rooms: List All", "request": { "method": "GET", "url": "{{base_url}}/rooms" } },
				{ "name": "Rooms: Detail", "request": { "method": "GET", "url": "{{base_url}}/rooms/1" } },
				{ "name": "Rooms: Similar", "request": { "method": "GET", "url": "{{base_url}}/rooms/1/similar" } },
				{ "name": "Rooms: Detect City", "request": { "method": "POST", "url": "{{base_url}}/rooms/detect-city" } },
				{ "name": "Rooms: Active Cities", "request": { "method": "GET", "url": "{{base_url}}/cities" } },
				{ "name": "Blogs: List", "request": { "method": "GET", "url": "{{base_url}}/blogs" } },
				{ "name": "Blogs: Detail", "request": { "method": "GET", "url": "{{base_url}}/blogs/test-slug" } },
				{ "name": "Pages: Content", "request": { "method": "GET", "url": "{{base_url}}/pages/privacy-policy" } },
				{ "name": "FAQ: List", "request": { "method": "GET", "url": "{{base_url}}/faq" } },
				{ "name": "Newsletter: Subscribe", "request": { "method": "POST", "url": "{{base_url}}/newsletter/subscribe" } },
				{ "name": "Contact: Submit Form", "request": { "method": "POST", "url": "{{base_url}}/contact" } },
				{ "name": "Offers: Active Banners", "request": { "method": "GET", "url": "{{base_url}}/offers" } }
			]
		},
		{
			"name": "2. USER (Sanctum Auth)",
			"item": [
				{ "name": "Get My Info", "request": { "method": "GET", "url": "{{base_url}}/auth/me" } },
				{ "name": "Logout", "request": { "method": "POST", "url": "{{base_url}}/auth/logout" } },
				{ "name": "Profile: Update", "request": { "method": "POST", "url": "{{base_url}}/profile/update" } },
				{ "name": "Profile: Delete OTP", "request": { "method": "POST", "url": "{{base_url}}/profile/delete-otp" } },
				{ "name": "Profile: Permanent Delete", "request": { "method": "DELETE", "url": "{{base_url}}/profile" } },
				{ "name": "Dashboard: Stats", "request": { "method": "GET", "url": "{{base_url}}/dashboard" } },
				{ "name": "Referral: Stats", "request": { "method": "GET", "url": "{{base_url}}/referral-stats" } },
				{ "name": "Wallet: Index", "request": { "method": "GET", "url": "{{base_url}}/wallet" } },
				{ "name": "Wallet: Convert Points", "request": { "method": "POST", "url": "{{base_url}}/wallet/convert" } },
				{ "name": "Wishlist: List", "request": { "method": "GET", "url": "{{base_url}}/wishlist" } },
				{ "name": "Wishlist: Toggle", "request": { "method": "POST", "url": "{{base_url}}/wishlist/toggle/1" } },
				{ "name": "Bookings: My List", "request": { "method": "GET", "url": "{{base_url}}/bookings" } },
				{ "name": "Bookings: Create New", "request": { "method": "POST", "url": "{{base_url}}/bookings" } },
				{ "name": "City Alerts: My List", "request": { "method": "GET", "url": "{{base_url}}/city-alerts" } },
				{ "name": "City Alerts: Subscribe", "request": { "method": "POST", "url": "{{base_url}}/city-alerts" } },
				{ "name": "City Alerts: Unsubscribe", "request": { "method": "DELETE", "url": "{{base_url}}/city-alerts/1" } },
				{ "name": "Plans: Browse Packages", "request": { "method": "GET", "url": "{{base_url}}/plans" } },
				{ "name": "Subscription: Purchase", "request": { "method": "POST", "url": "{{base_url}}/subscriptions/purchase" } }
			]
		},
		{
			"name": "3. OWNER (Role Owner)",
			"item": [
				{ "name": "Dashboard", "request": { "method": "GET", "url": "{{base_url}}/owner/dashboard" } },
				{ "name": "Payouts History", "request": { "method": "GET", "url": "{{base_url}}/owner/payouts" } },
				{ "name": "Enquiries (Leads)", "request": { "method": "GET", "url": "{{base_url}}/owner/enquiries" } },
				{ "name": "Rooms: My List", "request": { "method": "GET", "url": "{{base_url}}/owner/rooms" } },
				{ "name": "Rooms: Store New", "request": { "method": "POST", "url": "{{base_url}}/owner/rooms" } },
				{ "name": "Rooms: Toggle Status", "request": { "method": "POST", "url": "{{base_url}}/owner/rooms/1/toggle-status" } },
				{ "name": "Rooms: Make Featured", "request": { "method": "POST", "url": "{{base_url}}/owner/rooms/1/feature" } }
			]
		},
		{
			"name": "4. ADMIN (Role Admin)",
			"item": [
				{ "name": "Dashboard", "request": { "method": "GET", "url": "{{base_url}}/admin/dashboard" } },
				{ "name": "Analytics", "request": { "method": "GET", "url": "{{base_url}}/admin/analytics" } },
				{ "name": "Settings: Get", "request": { "method": "GET", "url": "{{base_url}}/admin/settings" } },
				{ "name": "Settings: Update", "request": { "method": "POST", "url": "{{base_url}}/admin/settings" } },
				{ "name": "Users: List", "request": { "method": "GET", "url": "{{base_url}}/admin/users" } },
				{ "name": "Users: Block/Unblock", "request": { "method": "POST", "url": "{{base_url}}/admin/users/1/toggle-block" } },
				{ "name": "Owners: List", "request": { "method": "GET", "url": "{{base_url}}/admin/owners" } },
				{ "name": "Owners: Detail", "request": { "method": "GET", "url": "{{base_url}}/admin/owners/1" } },
				{ "name": "Rooms: List All", "request": { "method": "GET", "url": "{{base_url}}/admin/rooms" } },
				{ "name": "Rooms: Approve", "request": { "method": "POST", "url": "{{base_url}}/admin/rooms/1/approve" } },
				{ "name": "Rooms: Reject", "request": { "method": "POST", "url": "{{base_url}}/admin/rooms/1/reject" } },
				{ "name": "Payments: List", "request": { "method": "GET", "url": "{{base_url}}/admin/payments" } },
				{ "name": "Payouts: Process", "request": { "method": "POST", "url": "{{base_url}}/admin/payouts/1/process" } },
				{ "name": "Plans: CRUD", "request": { "method": "POST", "url": "{{base_url}}/admin/plans" } },
				{ "name": "Blogs: CRUD", "request": { "method": "POST", "url": "{{base_url}}/admin/blogs" } },
				{ "name": "Offers: CRUD", "request": { "method": "POST", "url": "{{base_url}}/admin/offers" } },
				{ "name": "Reasons: CRUD", "request": { "method": "GET", "url": "{{base_url}}/admin/rejection-reasons" } },
				{ "name": "Pages: Update", "request": { "method": "PUT", "url": "{{base_url}}/admin/pages/privacy-policy" } },
				{ "name": "Search: Analytics", "request": { "method": "GET", "url": "{{base_url}}/admin/search-analytics" } }
			]
		}
	],
	"variable": [
		{ "key": "base_url", "value": "http://localhost:8000/api/v1", "type": "string" },
		{ "key": "token", "value": "token_here", "type": "string" }
	]
}
