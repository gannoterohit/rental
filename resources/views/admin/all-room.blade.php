@extends('layouts.admin')
@section('title','Room Listings')
@push('sweetalert')<script src="{{ asset('assets/js/sweetalert2.min.js') }}"></script>@endpush
@push('styles')<style>.room-filter{display:grid!important;grid-template-columns:minmax(190px,1fr) repeat(5,150px) auto!important;gap:8px}.room-table{min-width:1240px;width:100%}.room-table th,.room-table td{text-align:left!important;vertical-align:middle!important}.room-table th:last-child,.room-table td:last-child{text-align:right!important}@media(max-width:1279px){.room-filter{grid-template-columns:repeat(3,1fr)!important}}@media(max-width:767px){.room-filter{grid-template-columns:1fr!important}}</style>@endpush
@section('admin-content')
<div class="space-y-4 p-5 lg:p-6"><div class="flex items-end justify-between gap-3"><div><p class="text-[10px] font-extrabold uppercase tracking-[.2em] text-indigo-600">Property management</p><h1 class="mt-1 text-2xl font-extrabold">Room Listings</h1><p class="text-sm text-slate-500">Review, approve, reject and moderate every listing.</p></div><div class="flex gap-2"><a href="{{ route('admin.rejection-reasons.index') }}" class="rounded-xl border bg-white px-4 py-3 text-xs font-bold">Rejection reasons</a><a href="{{ route('admin.rooms.create') }}" class="rounded-xl bg-indigo-600 px-4 py-3 text-xs font-bold text-white"><i class="fas fa-plus mr-2"></i>Add room</a></div></div>
@if(session('success'))<div class="rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm font-semibold text-emerald-700">{{ session('success') }}</div>@endif
<form method="GET" class="room-filter rounded-2xl border bg-white p-4 shadow-sm"><input name="search" value="{{ request('search') }}" placeholder="Title or city" class="h-10 rounded-xl text-xs"><select name="listing_status" class="h-10 rounded-xl text-xs"><option value="">All approval states</option>@foreach(['pending'=>'Pending','approved'=>'Approved','rejected'=>'Rejected','expired'=>'Expired'] as $k=>$v)<option value="{{ $k }}" @selected(request('listing_status')===$k)>{{ $v }}</option>@endforeach</select><select name="moderation_status" class="h-10 rounded-xl text-xs"><option value="">All moderation</option>@foreach(['normal'=>'Normal','suspended'=>'Suspended','reported'=>'Reported'] as $k=>$v)<option value="{{ $k }}" @selected(request('moderation_status')===$k)>{{ ucfirst($v) }}</option>@endforeach</select><select name="status" class="h-10 rounded-xl text-xs"><option value="">Availability</option><option value="active" @selected(request('status')==='active')>Active</option><option value="booked" @selected(request('status')==='booked')>Rented / booked</option></select><select name="kyc" class="h-10 rounded-xl text-xs"><option value="">Owner KYC</option>@foreach(['pending','under_review','verified','rejected'] as $v)<option value="{{ $v }}" @selected(request('kyc')===$v)>{{ ucfirst(str_replace('_',' ',$v)) }}</option>@endforeach</select><select name="city" class="h-10 rounded-xl text-xs"><option value="">All cities</option>@foreach($cities as $city)<option value="{{ $city }}" @selected(request('city')===$city)>{{ $city }}</option>@endforeach</select><div class="flex gap-2"><button class="rounded-xl bg-slate-900 px-4 text-xs font-bold text-white">Filter</button><a href="{{ route('admin.all-rooms') }}" class="flex h-10 items-center rounded-xl border px-3 text-xs font-bold">Reset</a></div></form>
<form method="POST" action="{{ route('admin.rooms.bulk') }}" class="overflow-hidden rounded-2xl border bg-white shadow-sm">@csrf<div class="flex flex-wrap items-center justify-between gap-3 border-b bg-slate-50 px-5 py-3"><p class="text-xs font-bold">{{ $allrooms->total() }} listings · Showing {{ $allrooms->firstItem()??0 }}–{{ $allrooms->lastItem()??0 }}</p><div class="flex gap-2"><select name="action" required class="h-9 rounded-lg text-xs"><option value="">Bulk action</option><option value="approve">Approve</option><option value="suspend">Suspend</option><option value="activate">Activate</option><option value="mark_reported">Mark reported</option></select><button class="rounded-lg bg-indigo-600 px-4 text-xs font-bold text-white">Apply</button></div></div><div class="overflow-x-auto"><table class="room-table"><thead><tr><th><input id="selectAllRooms" type="checkbox"></th><th>Property</th><th>Owner / KYC</th><th>Location & rent</th><th>Approval</th><th>Moderation</th><th>Availability</th><th>Actions</th></tr></thead><tbody class="divide-y">@forelse($allrooms as $room)<tr><td class="px-4"><input type="checkbox" name="room_ids[]" value="{{ $room->id }}" class="room-check rounded"></td><td class="px-4"><div class="flex min-w-[220px] items-center gap-3"><div class="h-12 w-16 shrink-0 overflow-hidden rounded-lg bg-slate-100">@if($room->photo_url)<img src="{{ $room->photo_url }}" class="h-full w-full object-cover">@endif</div><div class="min-w-0"><a href="{{ route('admin.rooms.show',$room) }}" class="block max-w-[220px] truncate text-xs font-bold text-indigo-600">{{ $room->title }}</a><p class="text-[10px] text-slate-400">#{{ $room->id }} · {{ $room->roomTypeOption?->label??'Room' }}</p></div></div></td><td class="px-4"><a href="{{ route('admin.owners.detail',$room->owner) }}" class="text-xs font-bold">{{ $room->owner->name }}</a><p class="text-[10px] {{ $room->owner->verification_status==='verified'?'text-emerald-600':'text-amber-600' }}">{{ ucfirst(str_replace('_',' ',$room->owner->verification_status)) }}</p></td><td class="px-4"><p class="text-xs">{{ $room->city }}</p><p class="text-xs font-bold">₹{{ number_format($room->rent) }}/mo</p></td><td class="px-4"><span class="rounded-full px-2 py-1 text-[10px] font-bold {{ $room->listing_status==='approved'?'bg-emerald-50 text-emerald-700':($room->listing_status==='rejected'?'bg-red-50 text-red-700':'bg-amber-50 text-amber-700') }}">{{ ucfirst($room->listing_status) }}</span></td><td class="px-4 text-xs font-bold {{ $room->moderation_status==='normal'?'text-slate-500':'text-red-600' }}">{{ ucfirst($room->moderation_status) }}</td><td class="px-4 text-xs font-bold">{{ $room->expires_at?->isPast()?'Expired':ucfirst($room->status) }}</td><td class="px-4"><div class="flex justify-end gap-1"><a href="{{ route('admin.rooms.show',$room) }}" class="rounded-lg border px-2.5 py-2 text-xs" title="View"><i class="fas fa-eye"></i></a><a href="{{ route('admin.rooms.edit',$room) }}" class="rounded-lg border px-2.5 py-2 text-xs" title="Edit"><i class="fas fa-pen"></i></a>@if($room->listing_status!=='approved')<button type="button" class="approve-room rounded-lg bg-emerald-50 px-3 py-2 text-xs font-bold text-emerald-700" data-url="{{ route('admin.rooms.approve',$room) }}" data-title="{{ $room->title }}">Approve</button>@endif @if($room->listing_status!=='rejected')<button type="button" class="reject-room rounded-lg bg-red-50 px-3 py-2 text-xs font-bold text-red-700" data-url="{{ route('admin.rooms.reject',$room) }}" data-title="{{ $room->title }}">Reject</button>@endif</div></td></tr>@empty<tr><td colspan="8" class="p-12 text-center text-sm text-slate-500">No listings match these filters.</td></tr>@endforelse</tbody></table></div>@if($allrooms->hasPages())<div class="border-t p-4">{{ $allrooms->links() }}</div>@endif</form></div>
@endsection
@push('scripts')<script>
const csrf=document.querySelector('meta[name="csrf-token"]').content;
document.getElementById('selectAllRooms')?.addEventListener('change',e=>document.querySelectorAll('.room-check').forEach(c=>c.checked=e.target.checked));
document.querySelectorAll('.approve-room').forEach(b=>b.addEventListener('click',async()=>{const ask=await Swal.fire({title:'Approve this room?',text:b.dataset.title,icon:'question',showCancelButton:true,confirmButtonText:'Approve listing',confirmButtonColor:'#059669'});if(!ask.isConfirmed)return;const res=await fetch(b.dataset.url,{method:'POST',headers:{'X-CSRF-TOKEN':csrf,'Accept':'application/json'}});const data=await res.json();if(data.success){await Swal.fire('Approved',data.message,'success');location.reload()}else Swal.fire('Could not approve',data.message||'Please try again.','error')}));
const reasonOptions=@json($rejectionReasons->pluck('reason','id'));
document.querySelectorAll('.reject-room').forEach(b=>b.addEventListener('click',async()=>{const ask=await Swal.fire({title:'Reject room listing',text:b.dataset.title,input:'select',inputOptions:reasonOptions,inputPlaceholder:'Select rejection reason',showCancelButton:true,confirmButtonText:'Reject listing',confirmButtonColor:'#dc2626',inputValidator:v=>!v?'Please select a rejection reason.':undefined});if(!ask.isConfirmed)return;const res=await fetch(b.dataset.url,{method:'POST',headers:{'X-CSRF-TOKEN':csrf,'Accept':'application/json','Content-Type':'application/json'},body:JSON.stringify({reasons:[ask.value]})});const data=await res.json();if(data.success){await Swal.fire('Rejected',data.message,'success');location.reload()}else Swal.fire('Could not reject',data.message||'Please try again.','error')}));
</script>@endpush
@push('scripts')<script>
document.querySelectorAll('.approve-room').forEach(button=>{
    const row=button.closest('tr');
    const status=[...row.querySelectorAll('span')].find(item=>item.textContent.trim().toLowerCase()==='rejected');
    if(!status)return;
    const deleteButton=document.createElement('button');
    deleteButton.type='button';
    deleteButton.className='delete-room rounded-lg bg-red-600 px-3 py-2 text-xs font-bold text-white';
    deleteButton.innerHTML='<i class="fas fa-trash mr-1"></i>Delete';
    deleteButton.addEventListener('click',async()=>{
        const result=await Swal.fire({title:'Permanently delete this room?',text:button.dataset.title+' and its related listing data will be removed. This cannot be undone.',icon:'warning',showCancelButton:true,confirmButtonText:'Yes, delete permanently',confirmButtonColor:'#dc2626'});
        if(!result.isConfirmed)return;
        const deleteUrl=button.dataset.url.replace(/\/approve$/, '');
        const response=await fetch(deleteUrl,{method:'DELETE',headers:{'X-CSRF-TOKEN':csrf,'Accept':'application/json'}});
        const data=await response.json();
        if(data.success){await Swal.fire('Deleted','Rejected room has been permanently deleted.','success');location.reload();}
        else Swal.fire('Could not delete',data.message||'Please try again.','error');
    });
    button.parentElement.appendChild(deleteButton);
});
</script>@endpush
